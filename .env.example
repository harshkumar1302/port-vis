# Supabase (client — exposed to browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase (server — Vercel only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# Auth
JWT_SECRET=

# Email (contact form + cart order alerts)
RESEND_API_KEY=

# Cron keep-alive (Vercel)
CRON_SECRET=

# Google Analytics 4 (optional — e.g. G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=
