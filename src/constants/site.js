/** Primary studio contact — used across the site and order notifications */
export const SITE_EMAIL = 'visheshkalaofficial@gmail.com';

/** WhatsApp — country code + number, no + or spaces (for wa.me links) */
export const SITE_WHATSAPP_NUMBER = '917310956254';

/** Display format for UI */
export const SITE_WHATSAPP_DISPLAY = '+91 7310956254';

export default SITE_EMAIL;
